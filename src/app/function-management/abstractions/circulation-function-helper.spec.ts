import { CirculationFunctionHelper } from './circulation-function-helper';

xdescribe('CirculationFunctionHelper', () => {
  xit('should create an instance', () => {
    expect(new CirculationFunctionHelper()).toBeTruthy();
  });
});
